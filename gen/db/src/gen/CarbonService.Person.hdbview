-- generated by cds-compiler version 3.1.2
VIEW CarbonService_Person AS SELECT
  Person_0.email,
  Person_0.firstName,
  Person_0.lastName
FROM sap_bootcamp_Person AS Person_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN CarbonService_PersonCredential AS cred ON (cred.person_email = email),
  MANY TO MANY JOIN CarbonService_Road AS road ON (road.parent_email = email),
  MANY TO MANY JOIN CarbonService_Person2Room AS room ON (room.personID_email = email)
)