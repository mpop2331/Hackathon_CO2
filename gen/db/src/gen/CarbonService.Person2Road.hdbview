-- generated by cds-compiler version 3.1.2
VIEW CarbonService_Person2Road AS SELECT
  Person2Road_0.personID_email,
  Person2Road_0.roadID_roadID
FROM sap_bootcamp_Person2Road AS Person2Road_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN CarbonService_Person AS personID ON (personID.email = personID_email),
  MANY TO ONE JOIN CarbonService_Road AS roadID ON (roadID.roadID = roadID_roadID)
)