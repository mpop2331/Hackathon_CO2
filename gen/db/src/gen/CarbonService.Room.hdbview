-- generated by cds-compiler version 3.1.2
VIEW CarbonService_Room AS SELECT
  Room_0.roomID,
  Room_0.roomName,
  Room_0.owner
FROM sap_bootcamp_Room AS Room_0
WITH ASSOCIATIONS (
  MANY TO MANY JOIN CarbonService_Road2Room AS road ON (road.roomID_roomID = roomID),
  MANY TO MANY JOIN CarbonService_Person2Room AS person ON (person.roomID_roomID = roomID)
)