-- generated by cds-compiler version 3.1.2
COLUMN TABLE sap_bootcamp_Person (
  email NVARCHAR(5000) NOT NULL,
  firstName NVARCHAR(5000),
  lastName NVARCHAR(5000),
  PRIMARY KEY(email)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN sap_bootcamp_PersonCredential AS cred ON (cred.person_email = email),
  MANY TO MANY JOIN sap_bootcamp_Road AS road ON (road.parent_email = email),
  MANY TO MANY JOIN sap_bootcamp_Person2Room AS room ON (room.personID_email = email)
)