-- generated by cds-compiler version 3.1.2
COLUMN TABLE sap_bootcamp_Room (
  roomID NVARCHAR(36) NOT NULL,
  roomName NVARCHAR(5000),
  owner NVARCHAR(5000),
  PRIMARY KEY(roomID)
) WITH ASSOCIATIONS (
  MANY TO MANY JOIN sap_bootcamp_Road2Room AS road ON (road.roomID_roomID = roomID),
  MANY TO MANY JOIN sap_bootcamp_Person2Room AS person ON (person.roomID_roomID = roomID)
)