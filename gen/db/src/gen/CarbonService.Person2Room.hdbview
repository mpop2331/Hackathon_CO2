-- generated by cds-compiler version 3.1.2
VIEW CarbonService_Person2Room AS SELECT
  Person2Room_0.personID_email,
  Person2Room_0.roomID_roomID
FROM sap_bootcamp_Person2Room AS Person2Room_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN CarbonService_Person AS personID ON (personID.email = personID_email),
  MANY TO ONE JOIN CarbonService_Room AS roomID ON (roomID.roomID = roomID_roomID)
)