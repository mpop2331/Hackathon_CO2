-- generated by cds-compiler version 3.1.2
VIEW CarbonService_Road2Room AS SELECT
  Road2Room_0.roomID_roomID,
  Road2Room_0.roadID_roadID
FROM sap_bootcamp_Road2Room AS Road2Room_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN CarbonService_Room AS roomID ON (roomID.roomID = roomID_roomID),
  MANY TO ONE JOIN CarbonService_Road AS roadID ON (roadID.roadID = roadID_roadID)
)