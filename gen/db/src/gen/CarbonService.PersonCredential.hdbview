-- generated by cds-compiler version 3.1.2
VIEW CarbonService_PersonCredential AS SELECT
  PersonCredential_0.credID,
  PersonCredential_0.person_email,
  PersonCredential_0.password
FROM sap_bootcamp_PersonCredential AS PersonCredential_0
WITH ASSOCIATIONS (
  MANY TO ONE JOIN CarbonService_Person AS person ON (person.email = person_email)
)