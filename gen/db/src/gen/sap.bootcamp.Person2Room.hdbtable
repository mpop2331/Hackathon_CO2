-- generated by cds-compiler version 3.1.2
COLUMN TABLE sap_bootcamp_Person2Room (
  personID_email NVARCHAR(5000),
  roomID_roomID NVARCHAR(36)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN sap_bootcamp_Person AS personID ON (personID.email = personID_email),
  MANY TO ONE JOIN sap_bootcamp_Room AS roomID ON (roomID.roomID = roomID_roomID)
)